[package]
name = "ndn-did"
description = "Re-export shim — merged into ndn-security. Use ndn_security::did instead."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
ndn-security = { workspace = true }
