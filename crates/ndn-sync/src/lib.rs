/// State Vector Sync (SVS) — lightweight dataset synchronisation using a
/// distributed state vector to detect missing data between NDN nodes.
pub mod svs;

/// Partial Sync (PSync) — IBF-based dataset synchronisation.
pub mod psync;
